apiVersion: apps/v1
kind: Deployment
metadata:
  name: publisher-app
  labels:
    app: publisher-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: publisher-app
  template:
    metadata:
      labels:
        app: publisher-app
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "publisher-app"
        dapr.io/app-port: "8081"
        dapr.io/log-level: "info"
        dapr.io/config: "dapr-config"
        dapr.io/enable-api-logging: "true"
    spec:
      containers:
      - name: app
        image: publisher-app:latest
        imagePullPolicy: Never  # Use local image
        env:
        - name: PORT
          value: "8081"
        ports:
        - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: publisher-app
spec:
  selector:
    app: publisher-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  type: ClusterIP
